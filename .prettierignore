# Prettier has no ignore mechanism in .editorconfig and does not read .gitignore,
# so this file is still needed. node_modules is skipped by Prettier already.

# Generated by npm, and rewritten on every install.
package-lock.json

# Build output.
example/dist

# Copied verbatim from Fylgja so the tests exercise real source formatting.
tests/fixtures

# Scratch space for local tooling.
.delta
